import React from 'react';

export const metadata = {
  title: 'Comparador de cartões • Mapa do Crédito',
  description: 'Compare qual cartão compensa mais considerando anuidade e cashback.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
