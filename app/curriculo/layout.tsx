import React from 'react';

export const metadata = {
  title: 'Gerador de Currículo • Mapa do Crédito',
  description: 'Monte seu currículo em minutos e exporte em PDF.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
